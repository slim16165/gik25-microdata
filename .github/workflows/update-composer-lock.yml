name: Update composer.lock

on:
  workflow_dispatch: # Solo esecuzione manuale da GitHub UI (niente trigger automatici)

jobs:
  update-composer-lock:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0
      
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.1'
          extensions: mbstring, xml, curl, zip
          coverage: none
      
      - name: Cache Composer packages
        uses: actions/cache@v4
        with:
          path: vendor
          key: ${{ runner.os }}-php-${{ hashFiles('**/composer.json') }}
          restore-keys: |
            ${{ runner.os }}-php-
      
      - name: Validate composer.json (structure only)
        run: |
          echo "🔍 Validating composer.json structure..."
          composer validate --no-check-lock --strict || {
            echo "❌ composer.json has structural errors"
            exit 1
          }
          echo "✅ composer.json structure is valid"
      
      - name: Update composer.lock
        run: |
          echo "🔄 Updating composer.lock..."
          echo "📦 This may take a few minutes..."
          
          # Aggiorna composer.lock
          composer update --no-interaction --prefer-dist --no-scripts --with-all-dependencies
          
          # Verifica se composer.lock è cambiato
          if git diff --quiet composer.lock; then
            echo "✅ composer.lock is already up to date"
            exit 0
          fi
          
          echo "📝 composer.lock has been updated"
          echo "📋 Summary of changes:"
          git diff --stat composer.lock | head -20
          
          # Valida che composer.lock sia sincronizzato
          echo "✅ Validating updated composer.lock..."
          composer validate --strict || {
            echo "⚠️ Warning: composer.lock validation still has issues"
            echo "This might be due to package conflicts - review the changes"
          }
          
      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          git add composer.lock
          # [skip ci] evita che questo commit triggeri il workflow CI
          git commit -m "chore: Update composer.lock [skip ci]" || exit 0
          git push
          echo "✅ composer.lock updated and pushed successfully"
          echo "ℹ️  Note: This commit uses [skip ci] to avoid triggering CI workflow"

